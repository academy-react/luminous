import {type Locale } from "#/i18n.config";

const DashboardPage = ({
  params: { lang },
}: {
  params: { lang: Locale };
}) => {
  return (
    <div>dashboard</div>
  );
};
export default DashboardPage;
