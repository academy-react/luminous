import {type Locale } from "#/i18n.config";

const CourseDashboard = ({
  params: { lang },
}: {
  params: { lang: Locale };
}) => {
  return (
    <div>courses</div>
  );
};
export default CourseDashboard;
