import {type Locale } from "#/i18n.config";

const EditProfile = ({
  params: { lang },
}: {
  params: { lang: Locale };
}) => {
  return (
    <div>edit profile</div>
  );
};
export default EditProfile;
